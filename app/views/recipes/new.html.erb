<!-- /recipes/new -->

<%= render partial: 'shared/jumbotron', locals: { title: "Add a new recipe" } %>

<%= render partial: 'form', locals: { user: current_user, recipe: @recipe, categories: @categories, ingredients: @ingredients, measurements: @measurements } %>
