<!-- /recipes/new -->

<%= render partial: 'shared/jumbotron', locals: { title: "Add a new recipe" } %>

<%= render partial: 'form', locals: { recipe: @recipe, categories: @categories, ingredients: @ingredients, measurements: @measurements } %>
